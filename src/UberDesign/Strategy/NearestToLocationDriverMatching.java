package UberDesign.Strategy;

import UberDesign.Entity.Driver;
import UberDesign.Entity.Trip;

public class NearestToLocationDriverMatching implements DriverMatchingStrategy{
    @Override
    public Driver findDriver(Trip trip) {
        return null;
    }
}
